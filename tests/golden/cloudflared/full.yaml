---
# Source: cloudflared/templates/pdb.yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cloudflared
  namespace: cloudflare-system
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/component: tunnel
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared
      app.kubernetes.io/component: tunnel

---
# Source: cloudflared/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflared
  namespace: cloudflare-system
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/component: tunnel
  annotations:
    example.com/role: tunnel

---
# Source: cloudflared/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: cloudflare-system
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/component: tunnel
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared
      app.kubernetes.io/component: tunnel
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflared
        app.kubernetes.io/component: tunnel
      annotations:
        example.com/checksum: abc
    spec:
      serviceAccountName: cloudflared
      terminationGracePeriodSeconds: 30
      priorityClassName: system-cluster-critical
      nodeSelector: 
        kubernetes.io/arch: arm64
      tolerations: 
        - key: arch
          operator: Exists
      topologySpreadConstraints: 
        - labelSelector:
            matchLabels:
              app.kubernetes.io/name: cloudflared
          maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cloudflared
          image: "cloudflare/cloudflared:2026.8.3"
          imagePullPolicy: IfNotPresent
          command:
            - cloudflared
            - tunnel
            - --no-autoupdate
            - --metrics
            - 0.0.0.0:2000
            - run
            - --token-file
            - /secrets/tunnel-token
            - "--protocol"
            - "quic"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - name: metrics
              containerPort: 2000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ready
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 5
          resources: 
            limits:
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
          volumeMounts:
            - name: tunnel-token
              mountPath: /secrets
              readOnly: true
            - name: origin-ca
              mountPath: /etc/cloudflared/certs
              readOnly: true
      volumes:
        - name: tunnel-token
          secret:
            secretName: cloudflared-tunnel-token
        # Origin CA root (public cert). The remote tunnel config's
        # originRequest.caPool references /etc/cloudflared/certs/ca.pem,
        # so this mount must exist BEFORE the tunnel config stops using
        # noTLSVerify.
        - name: origin-ca
          secret:
            secretName: cloudflared-origin-ca
