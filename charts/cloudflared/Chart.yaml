apiVersion: v2
name: cloudflared
description: >-
  Cloudflare Tunnel daemon (cloudflared) as a plain Deployment — the
  in-cluster end of a remotely-managed tunnel, token from any Secret.
type: application
# Placeholder: the git tag is the sole version authority. The release
# workflow stamps both fields from the tag (truvity/ci-workflows
# release-public.yaml).
version: 0.0.0
appVersion: 0.0.0
home: https://github.com/truvity/cloudflare
sources:
  - https://github.com/truvity/cloudflare
keywords:
  - cloudflare
  - tunnel
  - cloudflared
maintainers:
  - name: Truvity
    url: https://github.com/truvity
